import React from 'react';
import PropTypes from 'prop-types';

export const FeedbackWidget = ({ state, good, neutral, bad }) => {
    return (
        state = {
  good: 0,
  neutral: 0,
  bad: 0
}
    );
}

